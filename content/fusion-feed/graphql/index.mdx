---
title: GraphQL API
children:
  - explorer
---

Example query:

```gql v2
query Foo($id: ID!) {
    node(id: $id) {
        __typename
    }
}
```

Click [here](https://graphql.org/learn/) to learn more about GraphQL.

Click [here](explorer) to go to the explorer.
